# Copy this file to ".env.local" and adjust if needed.
# Never commit the real ".env.local" file.

# Base URL of the FastAPI backend. Must be prefixed with NEXT_PUBLIC_
# so Next.js exposes it to browser (client-side) code.
NEXT_PUBLIC_API_URL=http://localhost:8000
